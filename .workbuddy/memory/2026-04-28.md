# 2026-04-28 工作日志

## 上午（12:07）- 新增监测公司列表页

**任务**：创建公司列表页，并把 index.html 的监控公司数字改为可点击

**已完成**：
1. 读取桌面 `标签.xlsx`，解析26家公司的标签数据（大脑架构/训练范式/场景标签/FMC³关联）
2. 新建 `companies.html` 列表页面：
   - 左侧：所有监测公司列表（国内+海外分区，支持搜索过滤）
   - 右侧：点击公司后显示对应标签卡片
   - 每家公司名称链接可跳转到 `company.html` 详情页
   - 已打标公司显示绿色小圆点
3. 修改 `index.html`：监控公司数字卡片改为可点击，点击跳转 `companies.html`

**标签覆盖**：26家公司（NVIDIA、Tesla Optimus、Figure AI、1X Technologies、Hexagon、Skild AI、Physical Intelligence、智元机器人、宇树科技、星尘智能、银河通用、苏度科技、星海图、至简动力、逐际动力、普渡机器人、灵心巧手、坤维科技+因时机器人（合并）、卧安机器人、光轮智能、Anybotics、Mimic Robotics、加速进化、帕西尼感知、穹彻智能、创新中心类）

**Commit**: `b04aa41`
- 已推送到 GitHub，Vercel 自动部署

**待续**：用户将继续告知打标逻辑，后续新增公司可自动打标

---

## 下午（12:28）- 重设计 companies.html 为简洁5列表格

**任务**：用户要求改成简洁表格，5列（公司名称/大脑架构/训练与数据范式/场景标签/定位）

**已完成**：
1. 读取桌面 `标签2.xlsx`，获取77家公司完整标签（4个维度）
2. 重写 `companies.html`：纯表格布局，支持搜索+分类筛选（海外/国内/各大脑架构类型）
3. 公司名称可点击跳转 company.html 详情页；定位列智能渲染 [Tag] 关键词高亮

**Commit**: `07366ac` - 已推送 GitHub

---

## 下午（12:36）- 修复 companies.html 数据空白 bug

**根本原因**：`allCompanies` 名称（如 `英伟达 (NVIDIA)`）与 `tagData` 名称（如 `NVIDIA (英伟达)`）格式不一致，导致 `getTag()` 全部返回 null，表格完全空白。

**修复方案**：直接将 `tagData` 派生为 `allCompanies`（加 `overseas` 字段），消除两套名单，`renderTable` 直接读取 `c.brain/training/scene/positioning`。

**Commit**: 本地已 commit，GitHub 网络不通需手动 push。

---

## 下午（13:51）- 优化 company.html 详情页 + 补充公司数据

**任务**：
1. 读取桌面 `标签逻辑.docx`，获取完整标签体系规范
2. 优化公司详情页，添加标签展示模块
3. 补充所有"待补充"的成立时间和总部信息

**已完成**：
1. 创建 `docs/标签体系SOP.md`：
   - Brain架构标签（E2E-VLA、World Model、Hierarchical等）
   - Training数据范式标签（IL-Teleop、Sim2Real-RL、Synthetic-Data）
   - Scene场景标签（I-Heavy、I-Precision、L-Logistics等）
   - 零部件标签（Dexterous Hand、Actuator、Force-Sensor等）
   - 新增公司打标流程

2. 更新 `company.html`：
   - 添加 `companyTagsMap` 标签映射（77家公司）
   - 更新 `rankingData` 补充38家公司的成立时间
   - 更新 `rankingData` 补充29家公司的总部信息
   - 在 `getCompanyInfo` 后备数据中添加 brain/training/scene 标签
   - 在详情页添加"技术标签"模块（三列展示大脑架构/训练范式/商业场景）
   - 添加"定位说明"模块

3. 更新 `MEMORY.md`：
   - 记录"新增公司必须打标签"为例行工作
   - 添加完整标签体系参考

**Commit**: 待推送
